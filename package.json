{
	"name": "node-sshclient",
	"version": "0.2.0",
	"description": "Lightweight ssh/scp client library for nodejs.",
	"keywords": ["ssh", "scp", "ssh client", "scp client", "node ssh", "node scp"],
	"author": {
		"name": "Gergely Varga",
		"email": "greg@sph3r.com",
		"url": "https://github.com/sperka"
	},

	"scripts": {
		"test": "mocha"
	},

	"main": "./index",

	"repository": {
		"type": "git",
		"url": "git://github.com/sperka/node-sshclient.git"
	},

	"bugs": {
		"url": "https://github.com/sperka/node-sshclient/issues"
	},

	"devDependencies": {
		"mocha": "*",
		"should": "*"
	},

	"engines": {
		"node": ">=0.8"
	}
}